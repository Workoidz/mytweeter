<p id="notice"><%= notice %></p>

<h1>Following Users</h1>

<table>
  <thead>
    <tr>
      <th>Following user</th>
     <!-- <th>User</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @following_users.each do |following_user| %>
      <tr>
        <td><%= following_user.following_user_id %> <%= User.find_by(id: following_user.following_user_id).name%> </td>
        <!--<td><%= following_user.user_id %> <%= User.find_by(id: following_user.user_id).name %></td>
        <td><%= link_to 'Show', following_user %></td>
        <td><%= link_to 'Edit', edit_following_user_path(following_user) %></td>
        <td><%= link_to 'Destroy', following_user, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<!--<%= link_to 'New Following User', new_following_user_path %>-->
