<%= form_with(model: follower_user, local: true) do |form| %>
  <% if follower_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(follower_user.errors.count, "error") %> prohibited this follower_user from being saved:</h2>

      <ul>
        <% follower_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :follower_user_id %>
    <%= form.text_field :follower_user_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
